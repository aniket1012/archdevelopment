import React from 'react'
import {
  AppBar,
  Toolbar,
  Typography,
  Button,
  IconButton,
} from "@material-ui/core";

const Header =(props)  => {


    return (
        <AppBar>
            <Toolbar>Arc Dev</Toolbar>
        </AppBar>
    )
}

export default Header
